version: '3.8'

services:
  modelscope-router:
    build: .
    container_name: modelscope-router
    ports:
      - "2166:2166"
    restart: unless-stopped
    volumes:
      - ./router_data:/app/refactored_router/router_data
